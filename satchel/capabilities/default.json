{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Satchel webview: core defaults plus window-close interception for the fund-safety exit gate (ExitGate.tsx).",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-destroy"]
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Satchel webview: core defaults plus window-close interception for the fund-safety exit gate (ExitGate.tsx), and OS notifications for swap events (issue #55).",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-destroy", "notification:default"]
}
